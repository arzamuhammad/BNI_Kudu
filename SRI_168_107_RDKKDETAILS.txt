
CREATE MULTISET TABLE PRD_EDW_SRI_TB.SRI_168_107_RDKKDETAILS ,FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO,
     MAP = TD_MAP1
     (
      ID BIGINT,
      RDKK_ID BIGINT,
      SUBCOMMODITY_ID INTEGER,
      IMPORT_FILE_ID INTEGER,
      QTY_P1 DECIMAL(20,3),
      QTY_P2 DECIMAL(20,3),
      QTY_P3 DECIMAL(20,3),
      STATUS_CARRYOVER INTEGER,
      RDKKDETAILS_ID_CARRYOVER BIGINT,
      QTY_CARRYOVER DECIMAL(20,3),
      QTY_ACC DECIMAL(20,3),
      QTY_SISA DECIMAL(20,3),
      CREATED_AT DATE FORMAT 'YYYY-MM-DD',
      UPDATED_AT DATE FORMAT 'YYYY-MM-DD',
      ACTION_BY INTEGER,
      IP_ADDRESS VARCHAR(35) CHARACTER SET LATIN NOT CASESPECIFIC,
      Start_Ts TIMESTAMP(6),
      End_Ts TIMESTAMP(6),
      Start_Date DATE FORMAT 'YYYY-MM-DD' NOT NULL,
      End_Date DATE FORMAT 'YYYY-MM-DD' DEFAULT DATE '9999-12-31',
      Record_Deleted_Flag BYTEINT DEFAULT 0 ,
      Ctl_Id SMALLINT,
      File_Id SMALLINT,
      Process_Name VARCHAR(26) CHARACTER SET LATIN NOT CASESPECIFIC NOT NULL,
      Process_Id INTEGER NOT NULL,
      Update_Process_Name VARCHAR(26) CHARACTER SET LATIN NOT CASESPECIFIC,
      Update_Process_Id INTEGER)
PRIMARY INDEX ( ID );